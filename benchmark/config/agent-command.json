{
  "schema_version": "1.0.0",
  "protocol": "jsonl-stdout-v1",
  "executable": null,
  "common_args": [],
  "arm_args": {
    "axi": [
      "--arm",
      "axi"
    ],
    "mcp": [
      "--arm",
      "mcp"
    ]
  },
  "required_fresh_session_args": [
    "--print",
    "--mode",
    "json",
    "--no-session"
  ],
  "required_disable_ambient_args": [
    "--no-extensions",
    "--no-skills",
    "--no-prompt-templates",
    "--no-context-files",
    "--no-themes"
  ],
  "credential_environment_variables": [
    "CODEX_LB_KEY"
  ],
  "notes": "Freeze a provider adapter before live execution. It must accept the complete prompt on stdin, emit durable provider JSONL on stdout, start a fresh non-persistent session, expose only arm-approved tools, disable ambient skills/hooks/MCPs, and never perform network access except provider API traffic. Pass credentials only through credential_environment_variables. See benchmark/docs/BENCHMARK.md for the authoritative toolchain-staging and process-lifecycle requirements."
}
