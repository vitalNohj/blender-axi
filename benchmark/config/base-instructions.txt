Complete the task using only the interface exposed for this benchmark arm. Work only inside the supplied writable workspace and output directories. Do not access the network, user configuration, user files, prior run directories, GUI automation, direct Blender addon sockets, or any unlisted tool. Do not install software or download assets. Preserve supplied scene data unless the task explicitly permits a change. Use deterministic values where the prompt leaves implementation detail open. At completion, report the work performed and relative artifact paths. A completion claim does not determine benchmark success.
